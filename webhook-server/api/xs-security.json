{
  "xsappname": "webhook-cap-server",
  "tenant-mode": "dedicated",
  "description": "Minimal XSUAA configuration for CAP service authentication",
  "oauth2-configuration": {
    "redirect-uris": [
      "https://*.hana.ondemand.com/**",
      "https://*.applicationstudio.cloud.sap/**",
      "http://localhost:5000/**",
      "http://localhost:5001/**"
    ]
  }
}
